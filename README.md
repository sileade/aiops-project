# AIOps Project with Telegram Control

Этот проект представляет собой полнофункциональную AIOps-систему для мониторинга, анализа и автоматического управления IT-инфраструктурой с помощью AI и Telegram.

## 🚀 Особенности

- **Комплексный Мониторинг:** Сбор логов и метрик из различных источников.
- **AI-Анализ:** Обнаружение аномалий и определение первопричин с помощью LLM.
- **Автоматическая Генерация Плейбуков:** Создание Ansible-плейбуков для исправления проблем.
- **Управление через Telegram:** Полный контроль над системой, включая утверждение действий, через удобный бот.
- **Микросервисная Архитектура:** Гибкая и масштабируемая система на основе Docker.

## 🛠️ Технический Стек

- **Бэкенд:** Python 3.11, FastAPI
- **Базы Данных:** Elasticsearch, Redis, Milvus
- **Мониторинг:** Prometheus, Fluentd
- **AI:** Hugging Face Transformers, Sentence Transformers
- **Автоматизация:** Ansible
- **Оркестрация:** Docker Compose

## ⚙️ Быстрый Старт

### 1. Предварительные требования

- Docker и Docker Compose
- Python 3.11+
- (Опционально) NVIDIA GPU с `nvidia-docker` для локального запуска AI-моделей.

### 2. Конфигурация

1.  **Создайте Telegram Бота:**
    - Поговорите с [@BotFather](https://t.me/BotFather) в Telegram, чтобы создать нового бота.
    - Скопируйте полученный токен.

2.  **Получите ваш Chat ID:**
    - Поговорите с ботом [@userinfobot](https://t.me/userinfobot), чтобы узнать ваш Chat ID.

3.  **Настройте `.env` файл:**
    - Скопируйте `.env.example` в `.env`:
      ```bash
      cp .env.example .env
      ```
    - Откройте `.env` и вставьте ваш `TELEGRAM_TOKEN` и `ADMIN_CHAT_ID`.

### 3. Запуск

Запустите все сервисы с помощью Docker Compose:

```bash
docker-compose up --build
```

Если у вас нет NVIDIA GPU, закомментируйте сервис `llm` в файле `docker-compose.yml` и укажите в `.env` эндпоинт для облачной модели.

### 4. Использование

- Откройте вашего Telegram-бота.
- Отправьте команду `/start`, чтобы начать.
- Используйте `/status` для проверки состояния системы.
- Запустите анализ с помощью `/analyze <имя_сервиса>`.

## 📁 Структура Проекта

```
/aiops_project
├── app/                # Основное приложение FastAPI
│   ├── api/            # API эндпоинты (если нужны отдельные)
│   ├── services/       # Бизнес-логика (анализ, автоматизация, AI)
│   ├── models/         # Pydantic модели
│   └── utils/          # Вспомогательные утилиты (логгер)
├── bot/                # Код Telegram бота
├── config/             # Конфигурация проекта
├── data/               # Данные (логи, плейбуки)
├── tests/              # Тесты
├── Dockerfile          # Dockerfile для сборки приложения
├── docker-compose.yml  # Файл для оркестрации сервисов
├── requirements.txt    # Зависимости Python
└── README.md           # Этот файл
```

## 🤖 Команды Бота

- `/start` - Начало работы и проверка доступа.
- `/status` - Получить полный отчет о состоянии всех компонентов системы.
- `/analyze <service_name>` - Запустить полный цикл анализа для указанного сервиса.
- `/pending` - Показать все планы, ожидающие утверждения.

## 💬 AI Чат-Бот

Проект теперь включает в себя AI чат-бот на базе модели `Qwen/Qwen2.5-32B-Instruct`. Вы можете управлять системой, отправляя команды на естественном языке прямо в чат с ботом.

### Примеры Команд

- `покажи статус системы`
- `проанализируй сервис my-app`
- `какие проблемы были за последний час?`
- `перезапусти nginx`

Система автоматически интерпретирует ваш запрос и выполнит соответствующее действие.

## 🐧 Интеграция с Proxmox VE

Система теперь поддерживает мониторинг и управление серверами виртуализации **Proxmox VE**.

### Возможности
- **Получение статуса** кластера, нод и отдельных VM.
- **Управление VM:** запуск, остановка, перезагрузка.
- **Анализ логов Proxmox** (требует настройки сбора логов в Elasticsearch).
- **Автоматическая генерация плейбуков** для решения проблем с VM (например, перезагрузка зависшей VM).

### Настройка

1.  **Создайте API токен в Proxmox** (`Datacenter` -> `Permissions` -> `API Tokens`).
2.  Добавьте следующие переменные в ваш `.env` файл:

    ```bash
    # Proxmox
    PROXMOX_HOST=your-proxmox-host
    PROXMOX_USER=root@pam
    PROXMOX_TOKEN_NAME=aiops_bot
    PROXMOX_TOKEN_VALUE=your-api-token-value
    ```

### Пример использования через Telegram

```
Вы: "покажи статус proxmox"

Бот: 🐧 **Статус кластера Proxmox:**
     - Ноды: 3
     - VM: 15
     - CPU: 45%
     - Память: 60%

Вы: "перезагрузи vm 101 на ноде pve1"

Бот: ✅ VM 101 на ноде pve1 перезагружается!
```


## 🔄 Замкнутый Цикл Обратной Связи (Closed-Loop Feedback)

Система теперь реализует полный замкнутый цикл обратной связи для каждого исправления, что гарантирует надежность и эффективность автоматизации.

### Этапы Цикла

1.  **Обнаружение:** Система находит аномалию в логах.
2.  **Анализ:** AI определяет причину и предлагает решение.
3.  **Утверждение:** Администратор получает уведомление и утверждает плейбук.
4.  **Выполнение:** Ansible запускает плейбук для исправления.
5.  **Пост-Анализ:** AI анализирует результат выполнения плейбука.
6.  **Верификация:** Система проверяет, исчезла ли исходная проблема из логов.
7.  **Отчет:** Администратор получает финальный отчет о результате.

### Преимущества

- **Надежность:** Каждое исправление проверяется.
- **Обучение:** Система может учиться на неудачных попытках.
- **Прозрачность:** Полный лог всех этапов для аудита.
