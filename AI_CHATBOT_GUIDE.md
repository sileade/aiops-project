# AI Чат-Бот для Управления Инфраструктурой

## 🤖 Обзор

AI чат-бот на базе модели **Qwen/Qwen2.5-32B-Instruct** позволяет управлять инфраструктурой, отправляя команды на естественном языке. Система автоматически интерпретирует ваши запросы и выполняет соответствующие действия.

## 🎯 Поддерживаемые Команды

### 1. Получение Статуса Системы

**Примеры:**
- "покажи статус системы"
- "как дела у системы?"
- "какой статус?"
- "проверь все компоненты"

**Ответ:**
```
Статус системы:
- API: ok
- Elasticsearch: ok
- Prometheus: ok
- Redis: ok
```

### 2. Анализ Сервиса

**Примеры:**
- "проанализируй сервис my-app"
- "запусти анализ для auth-service"
- "проверь payment-gateway"
- "анализ nginx за последний час"

**Параметры:**
- `time_window` — временное окно для анализа (по умолчанию 15m)
  - `5m` — последние 5 минут
  - `1h` — последний час
  - `24h` — последние 24 часа

**Ответ:**
```
Анализ для сервиса 'my-app' запущен.
```

### 3. Запуск Плейбука

**Примеры:**
- "перезапусти nginx"
- "запусти плейбук restart_nginx.yml"
- "выполни плейбук для auth-service"
- "перезагрузи сервер"

**Ответ:**
```
Запуск плейбука 'restart_nginx.yml' для 'nginx' пока не реализован.
```

### 4. Получение Логов

**Примеры:**
- "покажи логи сервиса api-gateway"
- "какие ошибки в логах payment-service?"
- "логи nginx за последний час"
- "есть ли проблемы в логах?"

**Параметры:**
- `time_window` — временное окно для логов
- `level` — уровень логирования (ERROR, WARNING, INFO)

**Ответ:**
```
Получение логов для 'api-gateway' пока не реализовано.
```

## 🔄 Как Это Работает

```
Пользователь отправляет сообщение
        ↓
Telegram бот получает сообщение
        ↓
Отправляет на API сервер (/chat эндпоинт)
        ↓
API сервер отправляет запрос в Qwen модель
        ↓
Qwen интерпретирует текст в JSON-команду
        ↓
API сервер обрабатывает команду
        ↓
Выполняется соответствующее действие
        ↓
Результат отправляется обратно в Telegram
        ↓
Пользователь получает ответ
```

## 📝 Примеры Диалогов

### Диалог 1: Проверка Статуса

```
Вы: покажи статус системы
Бот: Статус системы:
     - API: ok
     - Elasticsearch: ok
     - Prometheus: ok
     - Redis: ok
```

### Диалог 2: Анализ Проблемы

```
Вы: проанализируй сервис auth-service
Бот: Анализ для сервиса 'auth-service' запущен.

(Система анализирует логи и метрики)

Бот: ⚠️ Обнаружена аномалия в метриках auth-service: Аномально высокая загрузка CPU
     🚨 **Обнаружена Проблема: ...**
     Требуется ваше утверждение для выполнения плана.
     [✅ Утвердить] [❌ Отклонить]
```

### Диалог 3: Управление Сервисом

```
Вы: перезапусти nginx
Бот: Запуск плейбука 'restart_nginx.yml' для 'nginx' пока не реализован.

(После реализации)

Бот: 🚀 Начинаю перезапуск nginx...
     ✅ Nginx успешно перезапущен!
```

## 🧠 Как Qwen Интерпретирует Команды

Модель Qwen преобразует ваш текст в структурированную JSON-команду:

```json
{
  "action": "analyze_service",
  "target": "auth-service",
  "parameters": {
    "time_window": "15m"
  }
}
```

Доступные действия:
- `get_status` — получить статус
- `analyze_service` — запустить анализ
- `run_playbook` — запустить плейбук
- `get_logs` — получить логи

## ⚙️ Настройка

### Требования

1. **Модель Qwen:** Развернута на `LLM_ENDPOINT` (по умолчанию `http://localhost:8080`)
2. **FastAPI сервер:** Запущен на порту 8000
3. **Telegram бот:** Запущен и подключен к API

### Установка

```bash
# Убедитесь, что в .env установлены:
TELEGRAM_TOKEN=your_token
ADMIN_CHAT_ID=your_id
LLM_ENDPOINT=http://localhost:8080  # или облачный эндпоинт
```

### Запуск

```bash
# С Docker Compose
docker-compose up --build

# Или локально
python bot/main.py
```

## 🚀 Расширение Функционала

### Добавление Новой Команды

1. **Обновите промпт в `qwen_service.py`:**

```python
prompt = f"""...
Доступные команды (поле "action"):
- `get_status`: Получить статус системы или конкретного сервиса.
- `analyze_service`: Запустить анализ для сервиса.
- `run_playbook`: Запустить Ansible плейбук.
- `get_logs`: Получить логи для сервиса.
- `restart_service`: Перезапустить сервис.  # ← НОВАЯ КОМАНДА
...
"""
```

2. **Добавьте обработчик в `chatbot_service.py`:**

```python
elif action == "restart_service":
    # Логика для перезапуска сервиса
    return f"Перезапуск сервиса '{target}' запущен."
```

3. **Тестируйте:**

```
Вы: перезапусти my-service
Бот: Перезапуск сервиса 'my-service' запущен.
```

## 🔒 Безопасность

### Ограничения

- Только администратор (по `ADMIN_CHAT_ID`) может использовать чат-бот.
- Все команды логируются для аудита.
- Чувствительные операции требуют подтверждения.

### Best Practices

1. **Валидация:** Всегда валидируйте выходы AI-модели перед выполнением.
2. **Логирование:** Логируйте все команды и результаты.
3. **Ограничение Прав:** Используйте отдельные учетные записи для Ansible.
4. **Мониторинг:** Мониторьте использование чат-бота.

## 🐛 Решение Проблем

### Бот не отвечает на сообщения

1. Проверьте, что Telegram бот запущен: `docker-compose logs bot`
2. Проверьте, что API сервер запущен: `docker-compose logs api`
3. Проверьте, что `ADMIN_CHAT_ID` верен в `.env`

### Qwen не интерпретирует команду

1. Проверьте, что модель запущена: `docker-compose logs llm`
2. Проверьте, что `LLM_ENDPOINT` верен в `.env`
3. Попробуйте переформулировать команду на более простой язык

### Ошибка JSON при интерпретации

1. Проверьте логи: `docker-compose logs api`
2. Убедитесь, что модель возвращает корректный JSON
3. Может потребоваться переформулировка промпта

## 📊 Примеры Интерпретации

| Пользовательский ввод | Интерпретация |
|---|---|
| "покажи статус" | `{"action": "get_status", "target": "system"}` |
| "проанализируй api-gateway" | `{"action": "analyze_service", "target": "api-gateway"}` |
| "логи nginx за час" | `{"action": "get_logs", "target": "nginx", "parameters": {"time_window": "1h"}}` |
| "перезапусти postgres" | `{"action": "run_playbook", "target": "postgres", "parameters": {"playbook_name": "restart_postgres.yml"}}` |

## 🎓 Дальнейшее Развитие

### Возможные Улучшения

1. **Контекст Диалога:** Помнить предыдущие сообщения для более умных ответов.
2. **Обучение:** Улучшать интерпретацию на основе истории.
3. **Мультиязычность:** Поддержка команд на разных языках.
4. **Голосовые Команды:** Интеграция с голосовыми сообщениями.
5. **Предсказания:** Предлагать действия на основе истории.

---

**Наслаждайтесь управлением инфраструктурой через AI чат-бот!** 🚀
